{{ form_start(form) }}
    {{ form_widget(form) }}
  <button type="submit" class="rounded bg-blue-500 text-white font-bold py-2 px-4 hover:bg-blue-700 focus:outline-none focus:shadow-outline">
    {{ button_label|default('Save') }}
</button>

{{ form_end(form) }}
