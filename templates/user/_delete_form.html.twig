<form method="post" action="{{ path('app_user_delete', {'id': user.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ user.id) }}">
    <button type="submit" class="isk-rounded isk-bg-red-500 isk-text-white isk-font-bold isk-py-2 isk-px-4 isk-hover:bg-red-700 isk-focus:outline-none isk-focus:shadow-outline">
        Delete
    </button>
</form>