{% extends 'baseV1.html.twig' %}

{% block title %}Edit Family{% endblock %}

{% block body %}
<div class="p-11 rounded-xl bg-zinc-200">
    <div> 
        <h1 class=" flex justify-center text-3xl font-bold mb-9 text-sky-800">Edit Family</h1>
         
        
        <div>
            {{ include('family/_form.html.twig', {'button_label': 'Update'}) }}
            {{ include('family/_delete_form.html.twig') }}
        </div>

        <div class="flex justify-end mt-4">
            <a href="{{ path('app_family_index') }}" class="ml-4 mt-2 text-gray-500 hover:text-sky-700">back to list</a>
        </div>   
    </div>
</div>
{% endblock %}
